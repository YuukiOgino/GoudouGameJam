using UnityEngine;
using System.Collections;

public class GameManeger : MonoBehaviour {

	// Use this for initialization
	void Start () {
		AudioManager.Instance.PlayBGM("Unite In The Sky (short)");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
